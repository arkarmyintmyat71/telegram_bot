# Real local secrets — this file IS gitignored, never commit it.
#
# ⚠️ Your previous BOT_TOKEN here was a real, live token. It was replaced
# with a placeholder below — regenerate a fresh one via @BotFather (/revoke)
# and paste the new one in.
BOT_TOKEN=8829138453:AAEJxPMRUoRQLaHhBF9hmHtpCSvLSqLRxTY

ADMIN_IDS=1678659382

# Local dev: plain SQLite, zero setup.
# `${{ Postgres.DATABASE_URL }}` (Railway's variable-reference syntax) only
# works inside Railway's own dashboard — it is not valid in a local .env file
# loaded by python-dotenv, so it's been swapped out here.
DATABASE_URL=postgresql://neondb_owner:npg_lCPSUbfVD0R9@ep-withered-night-azibnxdj-pooler.c-3.ap-southeast-1.aws.neon.tech/telegram_bot?sslmode=require&channel_binding=require
MESSAGE_RETENTION_DAYS=30
CLEANUP_INTERVAL_HOURS=24
WEBSITE_URL=https://example.com
